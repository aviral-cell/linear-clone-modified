/**
 * Candidate Contract Surface — frontend
 *
 * This repo does not expose any frontend candidate task surface.
 *
 * The file is intentionally empty and exists only so the main-branch Knip
 * entry configuration stays structurally aligned with the golden-set pattern.
 */
